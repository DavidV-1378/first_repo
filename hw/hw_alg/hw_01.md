## Homework 

### H1 — vocabulary from examples

For `count_negative`, write the input, output, and one edge case in ordinary language.

### H2 — trace a sum

Trace the state of `total` for `[6, -2, 4]` and predict the result before running.

### H3 — first long word

Return the first word longer than five characters or `None`.

Required process: three examples, numbered steps, code, tests.

### H4 — repair early return

Repair:

```python
def any_negative(values: list[int]) -> bool:
    for value in values:
        if value < 0:
            return True
        return False
```

Smallest useful counterexample: `[1, -1]`. Move `return False` after the loop.

### H5 — integrator: first error log

A log entry is `(timestamp, level, message)`. Return the index and entry for the first `"ERROR"`, plus the number of entries checked. If there is no error, return `None` and the total checked.

Required evidence:

- input/output statement;
- ordinary, empty, first-error, later-error, and no-error examples;
- numbered steps;
- one trace table;
- tests.